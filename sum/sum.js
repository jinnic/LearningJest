/**
* write a simple sum function
* sum function takes two number and returns sum of given number
*/

//write you function inside 
const sum = (a, b)=> {
  return a + b;
}

// To test code, type the below command into your terminal
// npm test -- sum  /or/ jest sum 

module.exports = sum;
